node index.js 
	to run website
npm run build-react
	To convert the JSX in my developmentfiles to regular javascript for deployment
	The actual command is written in cast-everywhere's package.json that is way we call the command through npm 
	(check that the files are actually being converted) 